--- /tmp/style.orig	2026-05-25 13:27:37.090022279 +0000
+++ presentation/style.css	2026-05-25 13:29:07.953856752 +0000
@@ -177,6 +177,18 @@
   font-size: clamp(17px, 1.65vw, 24px);
 }
 
+.metric {
+  display: inline-block;
+  margin-top: 14px;
+  padding: 8px 13px;
+  border: 1px solid oklch(31% 0.035 270);
+  border-radius: 999px;
+  color: var(--green);
+  background: oklch(10% 0.018 260 / 0.52);
+  font-size: clamp(15px, 1.35vw, 19px);
+  letter-spacing: 0.01em;
+}
+
 .state-vector sub,
 .equation sub {
   font-size: 0.68em;
@@ -235,6 +247,62 @@
   animation: scan 5s var(--ease) infinite alternate;
 }
 
+.map-grid {
+  position: absolute;
+  inset: 5.8% 10.2% 10.8% 10.2%;
+  opacity: 0.20;
+  background-image:
+    linear-gradient(var(--line) 1px, transparent 1px),
+    linear-gradient(90deg, var(--line) 1px, transparent 1px);
+  background-size: 24px 24px;
+  mix-blend-mode: screen;
+  pointer-events: none;
+}
+
+.map-axis,
+.map-legend {
+  position: absolute;
+  z-index: 2;
+  border: 1px solid oklch(31% 0.035 270 / 0.82);
+  background: oklch(8% 0.015 260 / 0.72);
+  color: var(--text);
+  box-shadow: 0 12px 42px oklch(2% 0.01 260 / 0.38);
+  backdrop-filter: blur(7px);
+}
+
+.map-axis {
+  padding: 6px 10px;
+  border-radius: 999px;
+  font: 700 15px ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
+  color: var(--cyan);
+}
+
+.map-x {
+  left: 50%;
+  bottom: 1.5%;
+  transform: translateX(-50%);
+}
+
+.map-y {
+  left: 0.5%;
+  top: 50%;
+  transform: translateY(-50%) rotate(-90deg);
+}
+
+.map-legend {
+  right: 4.5%;
+  top: 5%;
+  padding: 10px 13px;
+  border-radius: 14px;
+  color: var(--gold);
+  font-size: clamp(13px, 1.25vw, 16px);
+  line-height: 1.25;
+}
+
+.map-legend span {
+  color: var(--muted);
+}
+
 .chrome {
   position: fixed;
   left: 24px;
@@ -371,6 +439,12 @@
     max-height: 100%;
   }
 
+  .map-grid,
+  .map-axis,
+  .map-legend {
+    display: none;
+  }
+
   .chrome {
     grid-template-columns: 1fr auto 1fr;
   }
